# pre-commit-sql-formatter
pre-commit hook to use sql-formatter

# Development

to run hook locally

```
pre-commit try-repo . sql-formatter --verbose --all-files
```

to tag

```
git tag v0.1.0
git push origin v0.1.0
```
